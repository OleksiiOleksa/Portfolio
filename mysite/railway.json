"startCommand": "while ! pg_isready -h $DB_HOST -p $DB_PORT -U $DB_USER; do sleep 1; done && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn mysite.wsgi --bind 0.0.0.0:$PORT"
 